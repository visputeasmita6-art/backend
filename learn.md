## step 1. node.js installation 

    - https://nodejs.org/en/download


## step 2. VS code dev setup 

- Created backend name folder 
- open with vs code 
- npm -v check in terminal 
- node -v
- 

## step-3: Backend creation with Node and express js

- npm init
- Above command creates package.json and it contains all dependencies for backend which we will create
- created index.js file which will be the main entry point


## Step-4: How to run Backend or normal javascript file with the help of node

- diffrence betwwen node and nodemon
    to run normal file we need node.js and to see continuous changes we installed nodemon 
    with help of command npm i nodemon -g 

- we run code for normal js file : node file path 
- so we run  backend :

        - we write a script in package.json
        - we wrote two scripts one for dev and o0ne for start
        - to run the scripts we go to main folder in terminal where package.json exists npm run --scriptkey--


## step 5: Installation of express.js for creating backend server

- npm i express 
- so above command installs express for backend server dependencies we can check it in package.json
- after installation we have import express module index.js which is main entry point

sequence of lines
    third line
    9 line
    we sent request to server using send method 
    we use get to get message on http 
     to get output in browser localhost:port_no




